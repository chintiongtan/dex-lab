import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /**
   * @NOTE dlb_2-step_1: setup asset prefix for remote app
   */
  assetPrefix: '/shop-static'
};

export default nextConfig;
